using UnityEngine;
using UnityEngine.UI;

public class NcoinsText : MonoBehaviour
{
    /*Text ncoinsText;

    // Start is called before the first frame update
    void Start()
    {
        ncoinsText = GetComponent<Text>();
    }

    // Update is called once per frame
    void Update()
    {
        //Set the current number of coins to display
        if (ncoinsText.text != PlayerMovement.SceneNCoins.sceneNcoins.ToString())
        {
            ncoinsText.text = ("Collect"+ Coin.totalCoins.ToString() + "Coins to next level.");
        }
    }*/
}